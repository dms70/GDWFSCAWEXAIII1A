{# {% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}

    <h1>Register</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn">Register</button>
    {{ form_end(registrationForm) }}
{% endblock %} #}




{# templates/security/login.html.twig #}
{% extends 'base.html.twig' %} 
{% block title %}REGISTER{% endblock %} 


{% block header %}
<nav class="navbar navbar-expand-lg navbar-light" >
  <a class="navbar-brand" href="#">Création d'une appication web avec une BDD</a>
  <a class="navbar-brand" href="#">Réalisé par David MARCAIS en 2021</a>
  <a class="navbar-brand" href="#">En Formation Avec :  </a>
  <a href="https://www.studi.fr/" style="color:#001aff;"> STUDI </a>
<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
  <span class="navbar-toggler-icon"></span>
  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-three-dots" viewBox="0 0 16 16">
  <path d="M3 9.5a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3zm5 0a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3zm5 0a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3z"/>
  </svg>
</button>

  <div id="navbarContent" class="collapse navbar-collapse justify-content-end">
            <ul class="navbar-nav nav-tabs nav-pills nav nav-fill  ">
              <li class="nav-item">
              <a class="nav-link " href="{{ path('app_visitor') }}">Visitor</a>
              </li>
              <li class="nav-item">
              <a class="nav-link active" href="{{ path('app_login') }}">Login</a>
              </li>
              <li class="nav-item">
              <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
              </li>
            </ul>
  </div>
  </nav>   
{% endblock %}

{% block body %}
<div class="container">
  <div class="row">
    <div class="col-md-10 ml-md-auto">
      <div class="">
        <div class="card bg-light mb-3 mt-5" style="width: 800px;">
          <div class="card-body">
            <form class="form-horizontal" role="form" method="post">
              <AmpContent>
    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}
              
</NonAmpContent> <AmpContent>

              
</NonAmpContent>
              <div class="card-header mb-3">REGISTER</div>
              <div class="form-group">
                {{ form_start(registrationForm) }}
                <div class="col-md-12">
                {{ form_row(registrationForm.email) }}
                </div>
              </div>
              <div class="form-group">
              <div class="col-md-12">
                {{ form_row(registrationForm.plainPassword, {label: 'Password'}) }}
              </div>

              </div>
              <div class="form-group">
              <div class="col-md-12">
                 {{ form_row(registrationForm.agreeTerms) }}
              </div>
              
              </div>
              <div class="form-group">
                <div class="col-md-12">
                  <button type="submit" class="btn btn-primary">Register</button>
                  {{ form_end(registrationForm) }}              
                </div>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
  

</div>

{% endblock %}


